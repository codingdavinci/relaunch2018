{#
/**
 * @see paragraph.html.twig
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}
      <div class="winner__project--image">
        {{ content.field_award }}
        {{ content.field_project }}
      </div>
      <div class="winner__project-title--wrapper">
        {{ content.field_project }}
        <div class="winner__project-title">
          {% set nid = content.field_project['#items'].target_id %}
          {{ drupal_link(drupal_field('title', 'node', nid), 'node/' ~ nid) }}
        </div>
      </div>
    {% endblock %}
  </div>
{% endblock paragraph %}
